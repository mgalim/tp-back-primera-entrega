doctype html
html(lang='es')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title Rompiendo Códigos
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css', rel='stylesheet')
    style.
      body {
        background-color: #0a0a0a;
        color: #ffffff;
      }
      .title-container {
        background: linear-gradient(to bottom, #1a472a, #0a0a0a);
        padding: 4rem 0;
      }
      .main-title {
        font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
        font-size: 4rem;
        color: #ffffff;
        text-shadow: 0 0 10px #4caf50;
      }
      .chemical-symbol {
        background-color: #4caf50;
        color: #ffffff;
        padding: 0.5rem;
        border-radius: 4px;
        font-weight: bold;
      }

  body
    .container-fluid.title-container.text-center.mb-5
      h1.main-title
        | Bienvenido a 
        span.chemical-symbol Ro
        | mpiendo 
        span.chemical-symbol Co
        | digos
    
    .container
      .row.justify-content-center
        .col-md-8.text-center
          .d-grid.gap-3.d-sm-flex.justify-content-sm-center
            a.btn.btn-success.btn-lg.px-4(href='/products/catalog') Ver Catálogo
